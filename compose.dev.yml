name: Fyn Students - Developpement

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - 5173:5173
    volumes:
      - ./src:/app/src
      - ./static:/app/static
